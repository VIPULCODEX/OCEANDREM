.venv/
__pycache__/
*.pyc
.DS_Store
.vercel

# Raw real-data source files (real/data/MOSDAC/, real/data/*.nc) -- large
# (100s of MB - ~800 MB each), not meant for git. Derived, compact JSON
# (public/data_real.json) is committed instead; provenance/checksums for
# the raw files are committed in real/PROVENANCE.md + real/data/CHECKSUMS.sha256.
# Regenerate locally with: python -m real.export_real_data
MOSDAC/
*.nc
